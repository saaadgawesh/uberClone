import '../../../core/App_Imports/app_imports.dart';

class myTrips extends StatelessWidget {
  const myTrips({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text("myTrips"));
  }
}
